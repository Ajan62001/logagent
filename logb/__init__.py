"""logb — a tool-using AI agent for EDA-tool log debugging & root-cause analysis.

Self-contained (pure stdlib, no hard third-party deps). The agent reads logs,
consults a manual (RAG), runs skills, follows files referenced in logs, and
asks clarifying questions when a request is ambiguous.
"""

__version__ = "0.1.0"
